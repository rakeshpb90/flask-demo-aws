version: 0.2
env:
  variables:
    SCRIPT_DIR: "./test/scripts"

phases:
  build:
    commands:
      - cd ${CODEBUILD_SRC_DIR}/${SCRIPT_DIR}
      - chmod +x docker_image_check.sh
      - ./docker_image_check.sh "${APP}" "${IMAGE_NAME}" "${REGION}" "${ENV_NAME}"

artifacts:
  files:
    - '**/*'